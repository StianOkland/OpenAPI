import { Select } from '../../react-responsive-select';
import { IProps, IState } from '../../types/';
interface IArgs {
    event: KeyboardEvent;
    state: IState;
    props: IProps;
    RRSClassRef: Select;
}
export declare function handleEnterPressed({ event, state, props, RRSClassRef }: IArgs): void;
export {};
