import { Select } from '../../react-responsive-select';
import { IProps, IState } from '../../types/';
interface TArgs {
    event: KeyboardEvent;
    state: IState;
    props: IProps;
    RRSClassRef: Select;
}
export declare function handleKeyEvent({ event, state, props, RRSClassRef }: TArgs): void;
export {};
