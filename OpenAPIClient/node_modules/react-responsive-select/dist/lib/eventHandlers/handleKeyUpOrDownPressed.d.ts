import { Select } from '../../react-responsive-select';
import { IState } from '../../types/';
interface TArgs {
    state: IState;
    type: 'INCREMENT' | 'DECREMENT';
    RRSClassRef: Select;
}
export declare function handleKeyUpOrDownPressed({ state, RRSClassRef, type }: TArgs): void;
export {};
