import { Select } from '../../react-responsive-select';
import { IState, IProps } from '../../types/';
interface TArgs {
    event: MouseEvent | KeyboardEvent;
    state: IState;
    RRSClassRef: Select;
    props: IProps;
}
export declare function handleClick({ event, state, RRSClassRef, props }: TArgs): void;
export {};
